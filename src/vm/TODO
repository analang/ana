Dicussion:
  Enforcing "module" types, whereas only a class is exported. Think package
  like in Java. Right now, modules are no different from frames. Meaning, that 
  upon import an import statement, the frame must be executed.

  import Module(with, arguments, here)


6/3/2018
- For Each statements


6/10/2018
- Finished if else if else statement suite
  TODO:
    make class instances real instances in the VM. Right now properties
    can be added to the definition frame at runtime.


6/20
unquoted identifiers in object literals
object_literal.key access rather than objectliteral['key']