// This creates a memory leak because of a single dimension
// refcount-- in INITARRAY
//

function init()
{
  dynamic_value = {a: 1 * 5 };

  a = dynamic_value;

  data = [[1,2,3, dynamic_value.a], "Test"];
  
  return data;
}

init();
