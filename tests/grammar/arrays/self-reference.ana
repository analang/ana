function main()
{
  names = ["Ryan", "Ana"];

  names.push(names);

  return names;
}

data = main();
data.push(main());

maps = {};
maps['data'] = data;

//print(maps['data']);

maps['data'] = 1;

//print(maps['data']);

foreach(key in maps)
{
  //print(maps[key]);
}

print("----");
print(data);
print("----");

foreach(key in data)
{
  print(key);
  //print(key);
  //print(data);
}

