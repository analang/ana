class Base {
  
}

class Child : Base {
  function getBase()
  {
    return base;
  }
}

child = Child();

theBase = child.getBase();

name = 1;

// Allocate 
for(i = 0; i < 100; i++)
{
  data = [4 *4, 4 * 2];
}

print(theBase);
