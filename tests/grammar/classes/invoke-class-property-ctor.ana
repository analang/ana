class BaseAdapter {
}

class Adapter : BaseAdapter {
  public function Adapter() 
  {
    self.base = base;
  }
  public function adapt() { 
    return 1; 
  }
}

bag = Adapter();

print(
  bag.adapter()
);
