function prefix_inc(value)
{
  print("prefix_inc ++value, value is " + value);
  while(++value != 10)
  {
    print(value);
  }
}

function prefix_dec(value)
{
  print("prefix_dec --value, value is " + value);
  while(--value)
  {
    print(value);
  }
}

prefix_inc(0);
prefix_dec(5);
